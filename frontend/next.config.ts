/**
 * @file next.config.ts
 * @description Next.js configuration file.
 * Used to customize the build process, environment variables, and other framework settings.
 */

import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
